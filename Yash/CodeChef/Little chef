#include<stdio.h>
int main()
{
      int t;
      int arr[t];
      scanf("%d",&t);
      for (int i=0;i<t;i++)
          {
           scanf("%d",&arr[i]);
           for (int j=0;j<arr[i];j++);
               int a[]
          }

      return 0;
}
